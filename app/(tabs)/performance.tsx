import { View, Text } from "react-native";
import GlobalClasses from "../../constants/styles/global.classes";

const Performance = () => {
  return (
    <View className={`${GlobalClasses.bg} flex-1`}>
      <Text>Performance</Text>
    </View>
  );
};

export default Performance;
